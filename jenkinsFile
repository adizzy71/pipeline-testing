pipeline {
    agent any
    stages {
        stage("scm checkout") {
            steps {
                checkout scm
                echo "step 1"
                echo "step 2"
                echo "$env.GIT_COMMIT"
                bat 'set'
            }

        }
    }
}
