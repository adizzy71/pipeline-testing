pipeline {
    agent any
    stages {
        stage("scm checkout") {
            steps {
                echo "step 1"
                echo "step 2"
                echo "$env.GIT_COMMIT"
            }

        }
    }
}
